import ui.MainFrame;

public class Main {
    /**
     * This code instantiate a MainFrame (JFrame) object
     * @param args (Not used)
     */
	public static void main(String[] args) {
        /* This code creates a Java Frame */
        MainFrame mainFrame = new MainFrame();
    }
}